name: example
root: ~/

pre_window: echo "starting pane"
startup_window: development
startup_pane: editor

windows:
  - development:
      layout: main-vertical
      panes:
        - editor: nvim
        - server: cargo run
        - tests: cargo test
  - shell:
